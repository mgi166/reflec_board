<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, label: :name %>

  <br />

  <%= f.input :password, label: :password %>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
